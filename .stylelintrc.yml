extends: stylelint-config-standard-scss
rules:
  # BEM naming (block__element, block--modifier) is used throughout, to
  # match the minimal-mistakes theme's own class conventions.
  selector-class-pattern: "^[a-z][a-z0-9]*(-[a-z0-9]+)*(__[a-z0-9]+(-[a-z0-9]+)*)?(--[a-z0-9]+(-[a-z0-9]+)*)?$"
